# NURSING with LOLO requires no environment variables.
#
# Keep future secrets out of Vite client variables and out of Git. If a future
# server-side feature needs a credential, configure it in the Netlify dashboard
# and read it only inside a Netlify Function or other trusted server runtime.

